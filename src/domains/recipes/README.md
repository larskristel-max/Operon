# Recipes Domain

## Responsibility
- Own recipe intake concerns (selection, upload intake metadata, parse handoff contracts).
- Provide upload intake contracts that parser modules can plug into later.

## Current scope
- Upload intake API contract for planned formats (PDF, image, spreadsheet, text, BeerXML, Brewfather exports).
- Parse status remains `pending`; parsing/confirmation implementation is out of scope for this phase.
